using UnityEngine;

// Gimmic_JumpBlock.csで参照されているクラス
public class ActorGroundSensor : MonoBehaviour
{
    // このクラスは、アクターが地面に接触しているかを判定するのに使われることが一般的です。
    
    // 必要に応じて、地面接触状態を返すプロパティなどをここに追加します。
    // 例: public bool IsGrounded { get; private set; }
}